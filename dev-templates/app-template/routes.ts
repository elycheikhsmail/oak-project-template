import { Context, Router } from "https://deno.land/x/oak@v9.0.0/mod.ts";
const routes = new Router();
// import controller there 





// link routers and cond controllers
// routes.get("/", (ctx: Context) => indexController(ctx));










export { routes };













